# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
*.bak

# Node (if/when a build script lands)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
*.tgz
.eslintcache

# Env / secrets
.env
.env.*
!.env.example

# Local-only project notes
CLAUDE.md

# Claude Code local overrides (keep .claude/settings.json committable)
.claude/settings.local.json

# Scratch
/tmp/
/scratch/

# Build artifact — dist/viewer.html is regenerated on each `node build.js`
# and embedded into index.html as the VIEWER_HTML constant. No need to
# commit the intermediate.
/dist/

# Commit-message tempfiles (see workflow note in CLAUDE.md)
.commit-msg.tmp
.commit-msg.*
